import "../style/Navbar.scss"

function Navbar(){
    return (
        <nav className="Navbar">
            <a href="#">Home</a>
            <a href="#">Profile</a>
            <a href="#">Gallery</a>
            <a href="#">Settings</a>
        </nav>
    )
}

export default Navbar;